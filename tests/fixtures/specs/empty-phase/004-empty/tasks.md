# Tasks: Empty-Phase Spec

**Branch**: `004-empty`

## Phase 1: Setup

This phase intentionally has prose but NO task checklist lines, so the
per-phase taskList renders empty — the Jira 400 INVALID_INPUT regression input.
